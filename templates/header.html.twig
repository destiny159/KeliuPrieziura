<!DOCTYPE html>
<html>
    <header class="">
        <nav class="navbar navbar-expand-lg navbar-light bg-light">

            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="navbar-toggler-icon"></span>
            </button> <a class="navbar-brand" href="#">Kelių Priežūra</a>
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown">Keliai</a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                            <a class="dropdown-item" href="{{ path('road_section_index') }}">Kelių ruožai</a>
                        </div>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown">Darbai</a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                            <a class="dropdown-item" href="{{ path('job_cypher_index') }}">Darbų šifrai</a>
                            <a class="dropdown-item" href="{{ path('completed_jobs_index') }}">Atlikti darbai</a>
                        </div>
                    </li>

                </ul>
                <ul class="navbar-nav ml-md-auto">
                    <li class="nav-item active">
                        <div style="font-size: 1.5em">
                            <strong>{{ current['current_place'] }}</strong>
                        </div>
                    </li>
                </ul>
            </div>
        </nav>
    </header>
</html>
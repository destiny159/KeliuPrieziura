{% extends 'base.html.twig' %}

{% block title %}JobCypher{% endblock %}

{% block body %}
    <h1>JobCypher</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_cypher.id }}</td>
            </tr>
            <tr>
                <th>Cypher</th>
                <td>{{ job_cypher.cypher }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ job_cypher.name }}</td>
            </tr>
            <tr>
                <th>Measurement</th>
                <td>{{ job_cypher.measurement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('job_cypher_index') }}">back to list</a>

    <a href="{{ path('job_cypher_edit', {'id': job_cypher.id}) }}">edit</a>

    {{ include('job_cypher/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('road_section_delete', {'id': road_section.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ road_section.id) }}">
    <button class="fas fa-trash-alt">Ištrinti</button>
</form>
